# AGENTS.md

本仓库执行原则：MVP 优先、一次性交付、少打断用户。

## 1. 当前项目目标

1. 忠实复现 `prototype` 的核心页面效果。
2. 首页只做极简搜索（`song_title` / `subtitle` / `tags`）。
3. 后端仅保留 Vercel Serverless 查询 Mongo 的最小 API。
4. 前端支持手动添加临时数据到 `localStorage` 并参与展示。

## 2. 工作方式

1. 默认自主推进，不反复请求确认。
2. 先做能跑通的最小实现，再补测试。
3. 只改和当前目标直接相关的文件，避免无关重构。
4. 如出现高风险/不可逆操作，再升级确认。

## 3. 代码边界

1. 数据模型以 `docs/schema.md` 为准。
2. 本轮只做 song 文档，不引入 album 文档。
3. 不实现 Bangumi 自动同步；`bangumi_id` 仅作预留字段。
4. 不引入后台管理、用户系统、复杂任务调度。

## 4. 质量底线

1. 关键路径可运行（首页搜索 + 单曲展示 + API 查询）。
2. 至少包含：
- schema 校验测试
- API 路由测试
- 本地临时数据合并逻辑测试
3. 输出清晰的已知限制与下一步建议。

## 5. 强约束

绝对不要用任何实际前端 UI 元素展示“当前思路/推理过程”；  
这类内容最多只允许以注释形式出现，不可进入最终界面或接口输出。
